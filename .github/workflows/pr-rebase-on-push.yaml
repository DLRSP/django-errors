---
name: Rebase PR (push)

on:
  push:
    branches:
      - main

permissions:
  contents: write
  pull-requests: write

jobs:
  rebase:
    uses: DLRSP/workflows/.github/workflows/pr-rebase-on-push.yaml@main
    secrets: inherit
